export enum Method {
  GET = 'GET',
  POST = 'POST',
  PUT = 'PUT',
  PATCH = 'PATCH',
  DELETE = 'DELETE',
}

export enum Themes {
  LIGHT = 'light',
  DARK = 'dark'
}

export enum Toast {
  ERROR = 'error',
  WARNING = 'warn',
  SUCCESS = 'success'
}

export enum Role {
  CLIENT = "client",
  ADMIN = "admin"
}

export enum ModalUse{
  ADD = "add",
  EDIT ="edit"
}